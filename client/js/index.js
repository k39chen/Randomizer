// Meteor subscriptions
//Meteor.subscribe("tags");

// define router configuration
// ...

// define routes
// ...

Router.map(function(){
	this.route("app", {
		path: "/",
		template: "app"
		/* yieldTemplates */
	});
});
